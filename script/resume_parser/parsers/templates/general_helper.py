#!/usr/bin/env python
# -*- coding: utf-8 -*-

def tag_eduinfo(text):
    """
    itype: text unicode
    rtype: [line_tags, tags_text]
        line_tags: list[list[tags]]
        tags_text: list[list[text]]
    desc: this function is to tag each item from text and
        return the list tag and the same struct text
    """
    return None, None

def handle_edu_tags(line_tags, tag_text):
    """
    itype: line_tags: list[list[tags]]
    itype: tag_text : list[list[text]]
    rtype: [line_tags, tags_text]
        line_tags: list[list[tags]]
        tags_text: list[list[text]]
    desc: this function is to handle the tags with probility
    """
    pass

def split_eduexp_block(line_tags, tag_text):
    """
    itype: line_tags: list[list[tags]]
    itype: tag_text : list[list[text]]
    rtype: [line_tags, tags_text]
        line_tags: list[list[tags]]
        tags_text: list[list[text]]
    desc: this function is to split the taged text to exps with its tags
    """
    pass

def extract_eduinfo(line_tags, tag_text):
    """
    itype: line_tags: list[list[tags]]
    itype: tag_text : list[list[text]]
    rtype: edu struct
    """
    pass



